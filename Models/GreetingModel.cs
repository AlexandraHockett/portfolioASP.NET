namespace portfolioASP.NET.Models
{
    public class GreetingModel
    {
        public string GreetingMessage { get; set; }

        public GreetingModel()
        {
            GreetingMessage = ""; // Ou qualquer outro valor padrão desejado
        }
    }
}
